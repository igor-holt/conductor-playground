Genesis Conductor monorepo. TypeScript pnpm workspace with apps/operator-web (Next.js 16 dashboard), apps/chatgpt-mcp (Node MCP app), packages/contracts (shared Zod schemas/mock data), packages/genesis-cli (CLI), packages/retraining-worker (Cloudflare Worker + D1), and packages/puf-harness (Python harness). Root README positions CLI as the only phase-1 write surface and root verify as lint + typecheck + tests + secret scan.